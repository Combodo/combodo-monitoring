# Name of profile (oql_select)
itop_user_select{toto="titi",profile="Administrator"} 1

# Nb of users (oql_count)
itop_user_count{toto="titi"} 1

# Nb of users (oql_groupby)
itop_user_groupby_count{profile="Administrator"} 1

# Retention count (conf)
itop_backup_retention_count{shadok="gabuzomeu"} 5

# custom class (custom)
itop_custom{baz="iste"} 42

# number of profiles
itop_profile_unique_count{} XXX

# iTop applicative version
itop_version{} ITOP_VERSION

# iTop after setup version (code + datamodel)
itop_setup_version{} ITOP_SETUP_VERSION

# Failed connections to a mailbox
itop_mailbox_failed_connection_count{} 0


